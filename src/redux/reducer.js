const initialData = {
    taskItems: [],
}

const taskReducer = (state = initialData, action) => {
    switch(action) {

        default:
            return state;
    }
}

export default taskReducer;